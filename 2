#include <stdio.h>

int main() {
    int arr[] = {27, -1, 12, -6, 5, 1, -3, 8};
    int n = 5; 
    int max = arr[0];

    for (int i = 1; i < n; i++) {
        if (arr[i] > max) {
            max = arr[i];
        }
    }

    printf("Наибольший элемент массива: %d\n", max);
  
    return 0;
}
